[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (in addition to public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
